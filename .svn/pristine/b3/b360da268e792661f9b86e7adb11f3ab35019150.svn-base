$("document").ready(function () {

    $("#form").submit(function () {
        if (confirm("Are you sure you want to delete this Category?")) {
            alert("Success!");
            return true;
        } else {
            return false;
        }
    
    })

})